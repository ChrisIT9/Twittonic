import { DateToTimeElapsedPipe } from './date-to-time-elapsed.pipe';

describe('DateToTimeElapsedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToTimeElapsedPipe();
    expect(pipe).toBeTruthy();
  });
});
