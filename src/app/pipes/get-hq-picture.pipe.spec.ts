import { GetHqPicturePipe } from './get-hq-picture.pipe';

describe('GetHqPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new GetHqPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
